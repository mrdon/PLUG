<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
    </plugin-info>

    <component key="preprocessor" class="com.atlassian.labs.plugins3.extender.Plugins3ApplicationContextPreProcessor"
               interface="com.atlassian.plugin.osgi.spring.external.ApplicationContextPreProcessor" public="true"/>

</atlassian-plugin>
